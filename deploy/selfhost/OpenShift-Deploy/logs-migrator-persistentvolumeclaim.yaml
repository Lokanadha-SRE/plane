apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: logs-migrator
  name: logs-migrator
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
